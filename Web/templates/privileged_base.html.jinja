{% extends 'base.html.jinja' %}

{% block base_content %}
  <style>
    .navbar {
      padding: 10px;
    }
  </style>

  <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div class="container">
      <b style="margin:5px">Welcome, {{current_user_username}}</b>
      <a class="btn btn-default" href="/" role="button">Dashboard</a>
      <a class="btn btn-default" href="/user/update-profile" role="button">Update profile</a>
      <a class="btn btn-default" href="/event/add" role="button">Add event</a>
      <a class="btn btn-default" href="/event/list" role="button">Event list</a>
      <ul class="nav navbar-nav navbar-right">
        <form id="logout" class="form-inline" role="form" action="/logout" method="post">
          <button type="submit" class="btn btn-default">Sign out</button>
        </form>
      </ul>
    </div>
  </nav>

  {% block privileged_base_content %}
  {% endblock %}

  
  <script>
      $(".profile-picture-container").each(function() {
        var source = $(this).find('p').html();
        if (source === "") {
          source = "http://upload.wikimedia.org/wikipedia/commons/thumb/6/68/Orange_Icon_User.svg/301px-Orange_Icon_User.svg.png"
        }
        var img = $(this).find('img')[0];

        // img.onload = function (e) {
        //     var canvas = document.createElement("canvas");
        //     var ctx = canvas.getContext("2d");
        //     ctx.drawImage(img, 0, 0, canvas.width, canvas.height);
        //     var url = canvas.toDataURL();
        //     // var output = img;
        //     // output.src = url;
        // };

        img.crossOrigin = 'Anonymous';
        img.src = source;
      });
  </script>
{% endblock %}